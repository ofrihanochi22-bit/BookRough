# Dependencies
node_modules/

# Build output
dist/
build/
out/
.vite/

# Environment / secrets (NEVER commit real env files — keep .env.example only)
.env
.env.local
.env.*.local
.env.development
.env.production
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test / coverage artifacts
coverage/
playwright-report/
test-results/
.playwright/

# Editor / OS
.DS_Store
Thumbs.db
*.swp

# Local Claude Code settings (keep shared settings.json, ignore per-user local overrides)
.claude/settings.local.json

# Claude Code local worktrees (scratch, never shared)
.claude/worktrees/
